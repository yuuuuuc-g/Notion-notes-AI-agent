# 🧠 AI Knowledge Agent

一个基于 Streamlit 和 LLM 的全能知识助手。它可以自动读取 PDF、网页链接或文本，识别内容类型（通用知识 vs 西语学习），并将结构化的笔记自动写入 Notion 数据库。

## ✨ 主要功能

1.  **多模态输入**：支持上传 PDF 文件、粘贴 URL 链接或直接输入文本。
2.  **智能路由 (Auto-Routing)**：
    * 自动判断内容是“西语学习材料”还是“通用知识”。
3.  **双模式处理**：
    * **🌍 通用模式**：生成摘要 (Summary)、提取标签 (Tags) 和核心知识点 (Key Takeaways 列表)。
    * **🇪🇸 西语模式**：提取核心单词、中文释义、例句表格，并生成学习摘要。
4.  **Notion 自动同步**：根据内容类型，自动写入不同的 Notion 数据库。
5.  **UI界面**：侧边栏操作，支持一键清空输入。

## 🛠️ 技术栈

* **前端**：Streamlit
* **核心逻辑**：Python (DeepSeek API)
* **存储**：Notion API
* **工具**：`python-dotenv`, `notion-client`, `PyPDF2` (或相关库)

## 📂 项目结构 (Project Structure)

本项目采用了模块化设计，各文件职责如下：

```text
.
├── app.py                # 🎨 前端入口 (Streamlit UI)
│                          - 处理侧边栏输入 (文件/文本)
│                          - 负责 UI 交互与日志显示
│                          - 调用 main_workflow 启动任务
│
├── main.py               # 🧠 核心业务逻辑 (The Brain)
│                          - 路由分发 (Router): 判断是西语还是通用内容
│                          - 提示词工程 (Prompts): 定义 LLM 的提取规则
│                          - 流程控制: 协调输入解析 -> LLM 处理 -> Notion 写入
│
├── notion_ops.py         # ✍️ Notion 操作模块 (The Hands)
│                          - 封装 Notion Client
│                          - create_study_note: 创建西语词汇表笔记
│                          - create_general_note: 创建通用知识点笔记
│
├── llm_client.py         # 🤖 模型接口层
│                          - 封装 OpenAI/DeepSeek API 调用
│                          - 统一处理 API 响应和错误
│
├── file_ops.py           # 📄 文件处理工具 (PDF 解析)
├── web_ops.py            # 🌐 网页处理工具 (URL 抓取)
├── requirements.txt      # 📦 项目依赖库
└── .env                  # 🔑 环境变量配置 (API Keys & DB IDs)

#git add .
#git commit -m "Fix: Remove parse_json to fix dict error"
#git push